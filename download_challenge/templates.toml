#
#   template.toml
#

[ mail ]

html = '''
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Weekly Challenge - <CHALLENGE></title>
<CSS>
</head>

<body class="light-mode">

<style>
    pre {
        background-color: #333; border-radius: 8px;
        padding: 21px; padding-left: 50pxs
    }
</style>
<style> .prose pre { padding: 10px; padding-left: 50px } </style>

<div class="page-hero">
    <div class="page-hero-inner">
        <h1 class="page-title">The Weekly Challenge: <CHALLENGE></h1>
                <svg xmlns="http://www.w3.org/2000/svg" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
                <PUBLISHED>
    </div>
</div>

<div class="content-wrap">
    <article class="prose">
<TASKS>
        <p><DEADLINE></p>
    </article>
</div>

</body>
</html>
'''

[code]

ch-N_pl = '''
#!/usr/bin/env perl
#
#       The Weekly Challenge - Perl & Raku
#       (https://theweeklychallenge.org)
#
#       Challenge <CHALLENGE> Task <TASK_NUMBER>: <TASK_NAME>
#
#       Perl solution by Matthias Muth.
#

use v5.36;

sub <SUB_NAME>( <INPUT_PARAMS> ) {
    my <RESULT_VAR>;
    return <RETURN>;
}

<RUN_TESTS>
'''

[readme]

readme_md = '''
# Challenge <CHALLENGE> tasks: <TASK_1_NAME> - <TASK_2_NAME>
**Challenge <CHALLENGE> solutions in Perl by Matthias Muth**

## Task 1: <TASK_1_NAME>

<TASK_1_Q_TEXT>

Lorem ipsum dolor sit amet...

```perl
sub <TASK_1_SUB>() {
    ...;
}
```

## Task 2: <TASK_2_NAME>

<TASK_2_Q_TEXT>

Lorem ipsum dolor sit amet...

```perl
sub <TASK_2_SUB>() {
    ...;
}
```

#### **Thank you for the challenge!**
'''

readme_no_blog = '''
**Challenge <CHALLENGE> solutions in Perl by Matthias Muth**
<br/>
(no blog post this time...)

**Thank you for the challenge!**
'''

blog_txt = '''
https://github.com/MatthiasMuth/perlweeklychallenge-club/tree/muthm-<CHALLENGE>/challenge-<CHALLENGE>/matthias-muth#readme
'''

test_all = '''
#!/usr/bin/env perl

use v5.36;

# Read the source file, remove testing code, and eval the rest.
my $source_file = "ch-1.pl";
my $code = do { local ( @ARGV, $/ ) = $source_file; <> }
    =~ s/^use Test2::V0.*//msr;
# Make sure we get a non-zero value back from the eval in case of success.
eval $code . "1;"
    or die "Can't parse $source_file:\n$@\n";

use Test2::V0 qw( -no_srand );
use Data::Dump qw( pp );

my $sub_name = "closest_palindrome";
my @tests = (
<TESTS>
);

sub run_test( $sub, $descr, $input, $output ) {
    if ( ! ref $output && $output =~ /^(?:(true)|false)$/i ) {
        my $expected_true = $1;
        $descr .=
            " $sub( " . join( ", ", map pp( $_ ), $input->@* ) . " )"
            . " is $output"
            if substr( $descr, -1, 1 ) eq ":";
        no strict 'refs';
        $expected_true
        ? ok $sub->( $input ), $descr
        : ok ! $sub->( $input ), $descr;
    }
    else {
        $descr .= " " . pp( $input ) . " => $output"
            if substr( $descr, -1, 1 ) eq ":";
        no strict 'refs';
        is $sub->( $input ), $output, $descr;
    }
}

# This runs the tests not only for the sub named "$sub_name",
# but also for all variants with any suffix ("$subname<suffix>").
for my $sub ( sort grep /^${sub_name}/, keys %:: ) {
    note "\n", "Testing $sub:\n", "\n";
    run_test( $sub, $_->@* )
        for @tests;
}

done_testing;
'''
